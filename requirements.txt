# Kernfunktionen (.eml-Verarbeitung, Mock-CRM, Reconciler, Notifier, SMTP,
# Weboberflaeche) laufen vollstaendig mit der Python-Standardbibliothek.

# Fuer den Import von Outlook-.msg-Dateien (Echtdaten): pure-Python, installiert
# sauber als Wheel. Ohne dieses Paket werden .msg-Dateien uebersprungen.
olefile>=0.47

# Optionaler Fallback-Parser fuer .msg (nur falls olefile nicht genutzt wird).
# extract-msg>=0.48
